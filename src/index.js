import React from 'react';
import ReactDOM from 'react-dom/client';

const element = <h1> ini ada angka perkalian {5 + 5}</h1>;

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(element);